# LLM Integration Test Configuration
# Copy this file to .env.test and fill in your values
# .env.test is gitignored and will not be committed

# Required: API key for LLM provider
LLM_TEST_API_KEY=your-api-key-here

# Required: Base URL for OpenAI-compatible API
LLM_TEST_BASE_URL=https://api.openai.com/v1

# Optional: Model name (defaults to gpt-4)
# Note: Tests use CHAT COMPLETION API only (no embeddings needed)
# Any OpenAI-compatible provider with chat API works (OpenAI, GLM, Ollama, etc.)
LLM_TEST_MODEL=gpt-4

# Neo4j Integration Test Configuration
# NEO4J_TEST_URL=neo4j://localhost:7687
# NEO4J_TEST_USER=neo4j
# NEO4J_TEST_PASSWORD=password
